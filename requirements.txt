scikit-learn>=1.3
rank_bm25>=0.2.2
numpy>=1.24

# --- Tier 2 (run locally / Colab with full internet access) ---
# sentence-transformers>=2.2   # for real dense embeddings (replaces LSA proxy)
# anthropic>=0.34              # for LLM answer generation (needs your own API key)
